# 标准库.歧

#### 内置类型
定义类型任意, 标记内部。  # 禁用类型检查

# 文本的默认值为 "" (空字符串)
定义类型文本, 标记内部。  # 字符串

# 数字的默认值全部为 0
# 抽象数字类型
定义类型数字, 标记内部。  # 包括所有数字类型, 默认相当于 js 的 Number
定义类型整数, 标记内部。  # 包括所有整数类型, 默认为 i64
定义类型实数, 标记内部。  # 包括所有浮点数类型, 默认为 f64
定义类型复数, 标记内部。  # 暂不支持

# 具体数字类型 (类似于 rust)
定义类型 u1, 标记内部。
定义类型 u8, 标记内部。
定义类型 u16, 标记内部。  # 暂不支持
定义类型 u32, 标记内部。
定义类型 u64, 标记内部。
定义类型 u128, 标记内部。  # 暂不支持
定义类型 u, 标记内部。  # usize, 暂不支持

定义类型 i8, 标记内部。
定义类型 i16, 标记内部。  # 暂不支持
定义类型 i32, 标记内部。
定义类型 i64, 标记内部。
定义类型 i128, 标记内部。  # 暂不支持
定义类型 i, 标记内部。  # isize, 暂不支持

# IEEE754 浮点数
定义类型 f16, 标记内部。  # 暂不支持
定义类型 f32, 标记内部。  # 暂不支持
定义类型 f64, 标记内部。
定义类型 f128, 标记内部。  # 暂不支持

# 类型别名
定义类型二, u1。

# 特殊数字
定义变量 NaN, 标记内部。
定义变量 Infinity, 标记内部。
定义变量非数, NaN。
定义变量无穷, Infinity。

# 高级类型
定义类型无, 标记内部。  # 函数无返回值
定义类型函数, 标记内部。
定义类型数组, 标记内部。  # [] array, list
定义类型映射, 标记内部。  # {} map, dict
定义类型集合, 标记内部。  # set

# 特殊值
定义变量空, 标记内部。

#### 运算符 (特殊的函数)

# 算术运算符
定义 + (数字), 参数某(数字), 标记内部、优先级(20)。  # 正
定义 - (数字), 参数某(数字), 标记内部、优先级(20)。  # 负

定义 + (数字), 参数甲(数字)、乙(数字), 标记内部、中位运算符、优先级(80)。  # 加 add
定义 - (数字), 参数甲(数字)、乙(数字), 标记内部、中位运算符、优先级(80)。  # 减 sub
定义 * (数字), 参数甲(数字)、乙(数字), 标记内部、中位运算符、优先级(70)。  # 乘 mul
定义 / (数字), 参数甲(数字)、乙(数字), 标记内部、中位运算符、优先级(70)。  # 除 div
定义 % (数字), 参数甲(数字)、乙(数字), 标记内部、中位运算符、优先级(70)。  # 余数 rem

# 位运算/逻辑运算
定义 ~ (整数), 参数(整数), 标记内部、优先级(20)。  # 非 not 运算
定义 ^ (整数), 参数甲(整数)、乙(整数), 标记内部、中位运算符、优先级(30)。  # 异或 xor 运算
定义 & (整数), 参数甲(整数)、乙(整数), 标记内部、中位运算符、优先级(30)。  # 与 and 运算
定义 | (整数), 参数甲(整数)、乙(整数), 标记内部、中位运算符、优先级(30)。  # 或 or 运算

定义变量非, (~)。
定义变量异或, (^)。
定义变量且, (&)。
定义变量或, (|)。

# 比较
定义 == (二), 参数甲(任意)、乙(任意), 标记内部、中位运算符、优先级(90)。
定义 != (二), 参数甲(任意)、乙(任意), 标记内部、中位运算符、优先级(90)。
定义 > (二), 参数甲(数字)、乙(数字), 标记内部、中位运算符、优先级(90)。
定义 < (二), 参数甲(数字)、乙(数字), 标记内部、中位运算符、优先级(90)。
定义 >= (二), 参数甲(数字)、乙(数字), 标记内部、中位运算符、优先级(90)。
定义 <=(二), 参数甲(数字)、乙(数字), 标记内部、中位运算符、优先级(90)。

定义变量等于, (==)。
定义变量不等于, (!=)。
定义变量大于, (>)。
定义变量小于, (<)。

#### 全局函数

# 基本输入输出
定义输出, 参数东西(任意), 标记内部。
定义写, 参数东西(任意), 标记内部。
定义换行, 标记内部。


# TODO
