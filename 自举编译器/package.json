{
  "name": "qc",
  "private": true,
  "version": "0.1.0-a1",
  "description": "歧语言自举编译器",
  "license": "GPL-3.0-or-later",
  "author": "sceext",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tpl-ol/qc.git"
  },
  "scripts": {
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check .",
    "build": "npm run fmt:check && npm run build:src && npm run build:ts",
    "build:src": "node build/build.js",
    "build:ts": "tsc"
  },
  "files": [
    "/lib"
  ],
  "bin": {
    "qc": "./lib/qc.js"
  },
  "devDependencies": {
    "@types/node": "^17.0.12",
    "@unicode/unicode-14.0.0": "^1.2.1",
    "prettier": "^2.5.1",
    "regenerate": "^1.4.2",
    "typescript": "^4.5.5"
  },
  "engines": {
    "node": ">=17.0",
    "npm": ">=8.0"
  }
}
