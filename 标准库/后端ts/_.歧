# 后端ts

标记内置:
  定义类型函数。
  定义类型任意, 外部(any)。
  定义类型空, 外部(null)。
  定义类型无, 外部(undefined)。
  定义类型文本, 外部(string)。
  定义类型数字, 外部(number)。
  定义类型整数, 外部(number)。
  定义类型实数, 外部(number)。

# 内置类型
导出默认函数。
导出默认任意。
导出默认空。
导出默认无。
导出默认文本。
导出默认数字。
导出默认整数。
导出默认实数。

# 数组
标记内置:
  定义类型数组, 泛型 T, 外部(Array):

    定义追加, 参数自己、元素(T):
      调用外部自己 “push” 元素。

    定义拼接(文本), 参数自己、分隔符(文本):
      调用外部自己 “join” 分隔符。
    
    # TODO

导出默认数组。

# 输出
定义输出, 参数内容(任意):
  调用外部 “console.log” 内容。

# 输出缓冲区
定义变量缓冲区1(数组(任意)), []。

定义写, 参数内容(任意):
  缓冲区1追加内容。

定义打印并清空缓冲区:
  定义变量残留, 缓冲区1拼接 “”。
  缓冲区1 = []
  输出残留。

定义换行:
  写 “\n”。
  打印并清空缓冲区。

导出默认输出。
导出默认写。
导出默认换行。
